# ViennaBar — notas de desarrollo en esta máquina

## Build
- Siempre compilar con `.\build.ps1` (inyecta `LIB`/`PATH` de VC++/SDK que NativeAOT requiere; sin eso `link.exe` falla con LNK1104 ucrt.lib).
- `dotnet` no está en PATH de sesiones nuevas: usar `C:\Program Files\dotnet\dotnet.exe`.
- `.\build.ps1 -Publish` publica el exe AOT (~2MB, `PublishAot=true` incondicional en el csproj desde F2.1b — el publish SIEMPRE es AOT). Pasar `-Project src\<spike>` para otro.

## Reglas CsWin32 aprendidas (S1)
- TFM obligatorio `net8.0-windows` + `<PlatformTarget>x64</PlatformTarget>` o el generator no emite nada.
- Firmas friendly del generado: `BeginPaint(hwnd, out ps)`, `FillRect(hdc, RECT*, HBRUSH)`, `SHAppBarMessage(ABM_NEW, ref abd)` — ver el generado en `obj\...\generated\` con `<EmitCompilerGeneratedFiles>true</EmitCompilerGeneratedFiles>` si hay dudas de firma.
- `WNDPROC`: función estática cast `(WNDPROC)WndProc` (NO `[UnmanagedCallersOnly]`, no convertible a delegate).
- Strings nativos: `fixed (char* s = "...")` → `PCWSTR`.
- `LRESULT`: `return default;`, no `return 0;`.
- **GUIDs COM: nunca escribirlos de memoria** — CsWin32 genera las constantes (`BHID_SFObject`, `CLSID_DragDropHelper`, …). Dos GUIDs manuales fallaron en F0 antes de adoptar esta regla.
- **Activación de apps del AppsFolder** (lección F1, costó 5 iteraciones):
  - Los AUMIDs (`{GUID}\app.exe`, `Microsoft.Windows.*`) NO se parsean con `SHCreateItemFromParsingName` ni contra el AppsFolder.
  - Ruta que funciona: PIDL snapshot del enum → `IShellFolder.GetUIObjectOf(IContextMenu)` del AppsFolder.
  - `InvokeCommand` REQUIERE `QueryContextMenu` previo (menú dummy con `CreatePopupMenu`) — sin él: E_INVALIDARG ("Value does not fall within the expected range" del wrapper).
  - `CMINVOKECOMMANDINFO` chico + `lpVerb` = "open" ANSI **persistente** (nada de stackalloc: el handler lee el LPCSTR después del retorno).
  - El E_INVALIDARG de COM llega como `ArgumentException` (no `COMException`) — catch genérico si se quiere log claro.

## Estado
- **F2.1b CERRADO (2026-09-02, noche 2)**: sesion de repro con VEH (CrashDiag.cs) + input sintetico (scripts en `%TEMP%\opencode\viennabar-repro\`). **Cero 0xC0000005 en ~15 corridas** (drop 4/4 OK, ctx menu 9/9 OK, framework-dependent Y AOT 2.08MB). El "crash" original NO se reprodujo; el hang intermitente de `QueryContextMenu` (drive) aparecio 1 vez en ~10 (siempre sobre "Windows (C:)", nunca en consola MTA/STA) — probablemente handler lento del drive; si vuelve, dumpear con `rundll32 comsvcs.dll,MiniDump <pid> <path> full` y leer con WinDbx/cdb (no hay debugger local).
- **Fixes de la sesion**: (1) WM_APP+2 (skin hot-reload) no tenia handler en WndProc — cableado; (2) la barra se ocultaba en mitad de un drag OLE (WM_MOUSELEAVE espurio del capture del drag helper) → `_dragActive` (seteado desde el CCW) + `CursorInsideWindow()` verifica el cursor real antes de ocultar; (3) panel **Drop Stack** visible en el tercio widgets (header con estado "suelta para apilar", items, feedback drag-over) — el CCW acepta toda la ventana, el panel la hace descubrible.
- **Lecciones repro/input sintetico**: `SetCursorPos` NO genera WM_MOUSEMOVE (usar `mouse_event` relativo); click sintetico a ventana inactiva NO se entrega (SetForegroundWindow antes); DoDragDrop de un spike externo necesita que la barra ya este revelada ANTES del click (los moves del drag no revelan); el usuario fisico moviendo el mouse EN MEDIO del repro pisotea el input sintetico (posiciones fallan por px) — repros UI automaticos solo confiables con la maquina quieta; `FindWindow` no ve ventanas TOOLWINDOW del appbar (EnumWindows por PID+clase); AOT real del exe = `dotnet publish src\ViennaBar -c Release -r win-x64 -p:PublishAot=true` (la linea `<PublishAot Condition=...>` del csproj es un no-op historico).
- F2.2 widgets (reloj+CPU+RAM). F2.3 ctx menu CERRADO (2026-09-03, prueba visual a mano): menu nativo legacy directo con 7-Zip fuera de "Mostrar mas opciones" (deseado); log sin hang en drive + carpeta (cmd=0 = dismiss sin seleccion: click-afuera/ESC; el menu se cierra solo al interactuar afuera, comportamiento nativo deseado).
- **F2 CERRADO (2026-09-04, 64 PASS headless)**: widgets 4-5 (discos fijos con GetDiskFreeSpaceEx + portapapeles con listener/historial-dedup-5/restore-on-click), settings (`Config.cs`: config.json + hot-reload WM_APP+3 con clamps, `width/revealMs/hideMs/skin`), skin packaging (`skins/<nombre>/skin.json` + fallback legacy + re-resolve al cambiar config; `skins/ViennaNight` de ejemplo en repo → copiar a `%APPDATA%\ViennaBar\skins\`), drop deferral (drop sobre carpeta del tree → menu Mover/Copiar/Apilar via `SHFileOperationW` raw con ALLOWUNDO; efecto COPY optimista en OnDrop + `DeferDrop` async por WM_APP+4 porque el IDataObject* muere al retornar). Lecciones: (1) CsWin32 sin sufijo W en el txt (`GetDriveType`, no `GetDriveTypeW`); `POINT` se mapea a `System.Drawing.Point` (no existe struct POINT); `string`→`PCWSTR` NO convierte (usar `fixed char*`); overloads friendly con `SafeHandle` confunden la resolución (construir HGLOBAL/HANDLE por `void*`: `(HGLOBAL)h.Value.ToPointer()`, `new HANDLE(new IntPtr(raw))`); (2) `GlobalAlloc` sin ZEROINIT exige terminador manual; (3) `FileOperation` + `fileop` stage prueban SHFileOperation silent con archivos reales en %TEMP%.
- **Fix redundancia visual CPU/RAM (2026-09-04, reporte con screenshot)**: la linea numerica suelta `"CPU x% RAM y%"` + barras duplicaba datos y, con las filas de disco entremedio, se leia como widget plano duplicado. Fix: % integrado en el label de cada barra (`"CPU 27%"`, mismo patron que `"C: 50 GB"`), linea eliminada (+20px para el stack). No era duplicado de codigo (un solo call site de pintado) sino de layout.
- **F3/M1 EN CURSO (2026-09-04)**: `Integration.cs` (ReadVerbs/ApplyM1/RevertM1 con backup EN registry `HKCU\Software\ViennaBar\M1Backup` — sin JSON por AOT; rescue `.reg` en escritorio), CLI `--m1-apply/--m1-revert`, single-instance (mutex + `WM_COPYDATA` raw con struct `CopyDataMsg` propio) + receptor que revela y `ExpandToPath` (raiz FS directa o via Este equipo por segmentos; testeado headless en `nav`). Stages `m1` (sandbox key, 10 checks) + `nav`. **Candidato (a) CONFIRMADO via oraculo COM `InvokeVerb("open")`**: el `shell.@="none"` de Win11 salta la resolucion de `shell\open` — hay que setear default `"open"` en Directory/Drive (con backup/restore del valor + linea en el rescue .reg) + `SHCNE_ASSOCCHANGED`; ahi el verbo SI llega (`open-folder:` en log x2). Lecciones: (1) tras cambiar verbs hay que `SHChangeNotify(SHCNE_ASSOCCHANGED)` o Explorer usa cache (nombres CsWin32: enums `SHCNE_ID`/`SHCNF_FLAGS`, no los miembros sueltos); (2) `%V` llega bien como args[1]; (3) `RegistryKey.GetValue("")` = valor default; (4) **backup WRITE-ONCE**: el 2do apply NO debe sobrescribir el backup (costo un envenenamiento real: revertir restauraba el override — fix + test `backup write-once`); (5) **forense primero**: `dump-verbs.ps1` (%TEMP%\opencode) dumpea HKCU+HKLM antes/despues — sin el, el `"none"` y el envenenamiento se ven tarde; (6) `cmd start` y `Start-Process` sobre dirs NO resuelven verbs (van a explorer directo, territorio M2 — la matriz ya lo decia); (7) `RevertM1.Prune` solo borra claves vacias (ZCode del usuario intacto). **Directiva usuario: M1 queda REVERTIDO y OFF hasta el final del proyecto** (la barra aun no es confiable para el control final; revert verificado por diff contra baseline). E2E usuario OK (doble-click intercepta la barra); maquina final limpia verificada (sin overrides, sin backups). F2.4 skin hot-reload + orbe Inicio. Panel Drop Stack (F2.5 parcial).
- **F2.5 alcance restante (2026-09-04)**: (1) drop de imagenes web (`FileGroupDescriptorW` + `FileContents` por indice, ISTREAM→HGLOBAL, snapshot a temp — hoy solo entra `CF_HDROP`); (2) semantica snapshot (copiar bytes al apilar, inmune a MOVE del original + `UniquePath`); (3) thumbnails etapa 1 via `IShellItemImageFactory` (ya en el txt del Shell, sin GDI). Tests: round-trip virtual→archivo + snapshot-dedup. Entra por `DropEngine.OnDrop`, sin tocar arquitectura.
- **F2.5 panel NUEVO en drawer + virtual files (2026-09-04, pedido usuario)**: panel del widgets ELIMINADO; superficie drop en el drawer colapsado (grid 3x thumbs 56px via `IShellItemImageFactory`→DIBits→BGRA premultiplicado→D2D, cache 40 en Renderer con purge en Resize/Dispose) + drag-out rewired al hit-test del drawer. Drawer auto-close: al invocar programa, con ESC vacio, o 4s idle (timer 4). Virtual files: `ReadVirtualFiles` (descriptor HGLOBAL + `FileContents` ISTREAM por vtable cruda slot 3 / fallback HGLOBAL, chunks sin confiar sizes) → snapshot a `%TEMP%\ViennaBar\drop\` (archivos REALES re-dropeables). Lecciones: (1) metodos de interfaz CsWin32 devuelven void+throw (no HRESULT como los P/Invoke); `HBITMAP` vive en `Graphics.Gdi` con `Value` nint; (2) `GetDIBits` top-down (`biHeight` negativo) evita flip; alfa: a==0→opaco, resto premultiplicar; (3) el parser del descriptor NO sanitiza (lo hace el ensamblado) — test falló por expectativa, no por codigo. Stages `virtual` + `thumb` (121 PASS headless).
- **Tree feedback + boton clasico + color (2026-09-04, pedido usuario con screenshot)**: tree con scroll (WM_MOUSEWHEEL, `_topRow` clamp), highlight de seleccion (`_selected`, se setea en click/navegacion/invoke), `EnsureVisible` (navegar lleva a la vista) + expandir destino tras Mover/Copiar del menu deferral. Boton Inicio rectangular estilo clasico abajo de todo (relieve luz/sombra) con ancla PNG definida (`LogoX/LogoPx` 24px, futura `start.png` del skin; orbe eliminado). Paleta Celeste con mas color (bg `DCE9F5`, divider/btn `2E7CC4`, etc. — tests de skin actualizados al default nuevo). 124 PASS.
- **Forense visual con screenshots propios (2026-09-04)**: el boton "ausente" se pintaba en `by=1040` con ventana de 1032 — `_clientH` usaba el alto del MONITOR (1080) pero el rect appbar real es 1032 (taskbar reserva 48px): layout entero shiftado. Fix: `_clientH` del rect post-`SETPOS`. Overlap de nombres largos = wrap de DWrite: fix `NO_WRAP` + ellipsis (`CreateEllipsisTrimmingSign`, leak intencional de 2 objetos). Tercios rebalanceados (widgets 190 fijo, tree flexible, drawer 344) — el tercio widgets quedaba 2/3 vacio. Verificado en foto: boton visible, ellipsis, Tools expandido. Lecciones vision: revelar via `--open-folder` + `stayOpen:true` en config (suprime hide) + captura CopyFromScreen; chequear frescura por timestamp/tamano.
- **WalkDown en ExpandToPath (2026-09-04, reporte usuario)**: la rama de raices FS solo expandia la raiz sin descender (`Downloads\Tools` quedaba en Descargas). Fix: `WalkDown` compartido (desciende por segmentos + expande el destino final) con match por `Name` O cola del `ParsingName` (carpetas localizadas). Test `nav` con 2 niveles bajo Escritorio. Verificado en log vivo: `seg=[Tools] -> Tools (23 hijos)`.
- **Reveal on DragEnter (2026-09-04)**: la barra oculta NO se revelaba al arrastrar desde el Escritorio (los moves del drag OLE no generan WM_MOUSEMOVE — leccion F2.1b; NO era efecto del revert M1/M2). Fix: `SetDragActive(true)` revela si estaba oculta.
- **Clipboard: wiring OK, historial volatil (2026-09-04)**: sonda `Set-Clipboard` → listener recibe y captura (22ch en log). El "desaparecio" = historial solo en memoria (cada relaunch por rebuilds lo borra) + misses por contencion del lock global (primer update con `open FAIL`). Fix: reintento x3 con 15ms + header `Clip` SIEMPRE visible (vacio = presente-sin-contenido).
- **Forense FindWindow fantasma (2026-09-04, saga con usuario)**: `FindWindow` por clase devolvia 0 con la ventana viva — el bug estaba en MIS probes, no en la app: PowerShell marshala `$null`→`""` en params string de P/Invoke Add-Type (con `""` nada matchea; con ambos strings reales si). En C# los null son NULL de verdad y el lookup funciona (probado en vivo N veces). Leccion: ante probe contradictorio (`EnumWindows` ve, `FindWindow` no), sospechar del harness antes que del target. Relacionado: `SendMessageTimeout` contra hilo en arranque tarda ~5s (cubre el inicio, entrega igual); en idle es instantaneo. Vision: screenshots via CopyFromScreen funcionan (Read devuelve la imagen); revelar con `--open-folder ""` + captura rapida; forzar geometria con `SetWindowPos` externo NO pinta (el RT de D2D solo sigue al `SetPos` de la app).
- **M2 primer apply vivo FALLIDO (2026-09-04, sesion con usuario)**: dos bugs encadenados. (1) Launcher como `Exe` = subsistema consola → cada invocacion abria ventana negra (huerfana al salir, se acumulaban: el "loop"). Fix: `WinExe`. (2) IFEO antepone la imagen original al command line: el launcher recibia `["explorer.exe", ...args]` y `Classify` miraba `args[0]` crudo → TODO iba a Passthrough y abria el Explorer real (cero forwards en el log lo delato). Fix: `StripImageName` (case-insensitive, con path completo) + tests. Leccion: ante "abre X pero con efecto lateral", mirar el log del receptor primero (ausencia de `open-folder:` = el forward nunca salio). Cold-start probado: launcher sale y la barra arranca sola.
- **M2 round 2: `C:\Temp` NO existe (2026-09-04)**: el "lleva a Documentos" no era bug — `Classify` manda inexistentes a Passthrough y Documentos es el fallback del propio Explorer ante paths basura. Probar M2 siempre con carpetas REALES. Forward de carpeta real verificado en vivo (`open-folder: ...\Temp` en log). Win+E simulado (`["C:\Windows\explorer.exe"]` → Reveal) tambien verificado.
- **M2 limite real: Win+E es in-process (2026-09-04, con usuario)**: 3x Win+E con argv-logging en el launcher = CERO invocaciones → no spawnea proceso (SeparateProcess=0 + reuso COM). IFEO es ciego por estructura, no por bug. `RegisterHotKey(MOD_WIN)` TAMPOCO sirve con Explorer vivo (first-registrant-wins: Explorer ya tiene Win+E/R; error 1409) — correccion del usuario. Opciones reales: hook WH_KEYBOARD_LL (contaminacion global, contra filosofia), policy NoWinKeys+reclamo (mata todos los Win+X), o M3 (limpia). **Win+E parqueado hasta M3**; M1 cubre doble-click. M2 revertido y verificado (IFEO vacio, sin backup).
- **F2.5 drag-out DEL STACK FUNCIONA (2026-09-03, verificado a mano)**: agarrar item del panel Stack y soltarlo afuera (Explorer/escritorio) entrega el archivo real. Pipeline: WM_LBUTTONDOWN con SetCapture → umbral 5px → `ShellNative.CreateDataObjectFromPaths` (SHCreateDataObject(null,0,null) + SetData CF_HDROP HGLOBAL fabricado a mano: DROPFILES 20B fWide=1 + paths double-null) → `DoDragDrop` raw con `DropSourceCcw` (CCW manual IDropSource, DRAGDROP_S_DROP 0x40100 / MOVE quita el item). Lecciones: (1) GUID ctor de 10 args (faltaba un 0) da CS1727/CS1729 — contar los 11 literales del target-typed `new(...)`; (2) `GMEM_MOVABLE` no existe como const generado → cast `(GLOBAL_ALLOC_FLAGS)0x0002`; (3) CsWin32 BOOL struct: `fNC = 0` no compila, usar `default`; (4) STGMEDIUM union se llama `_u_e__Union` en este CsWin32 (0.3.106); (5) el hit-test del panel guarda dimensiones en Render (StackHitTest usa `_stackPanelY/H` del último paint).
- **Suite headless (2026-09-03)**: `dotnet run --project src\ViennaBar.Shell.Tests -c Release -- headless` → 36 PASS sin GUI (enum-shell, dataobj round-trip CF_HDROP con unicode, CCW QI/refcount/state-machines vía vtable, skin parse+defaults, cache round-trip+corrupto). Stages nuevos: `dataobj ccw skin cache headless`. Lecciones: (1) Tests referencia al core WinExe sin problema (solo compila la dll, el entry point lo pone el proyecto Tests); el core expone internos vía `InternalsVisibleTo(ViennaBar.Shell.Tests)`; (2) en método `unsafe`, `&local` de un value-type NO necesita `fixed` (CS0213 si lo pones); (3) SHCreateDataObject+SetData+GetData funciona en MTA de consola, sin STA ni ventana; (4) CCWs devolvían `E_NOTIMPL` (-2147467263) en QI fallido — corregido a `E_NOINTERFACE` (-2147467262) y pineado por test.
- **Logging gated + ring (2026-09-03)**: hub `ShellNative.DebugLog` (visible al core por IVT) — ring de 256 en memoria SIEMPRE (Interlocked, cero I/O), archivo `%TEMP%\viennabar-app.log` SOLO con centinela `%TEMP%\viennabar-debug` (mismo patrón que `viennabar-kill`, evaluado una vez al arrancar). `App.AppLog`, `Renderer.AppLog`, `AppCatalog.Log`, `DropTargetCcw.Log`, `Main enter` delegan al hub; el shell usa `SLog` (tag `[shell]`), el CCW `[ccw]`. El VEH vuelca el ring al crash-log → forense completo sin I/O en steady-state. Stage `log` pinea orden/capacidad/sentinel (44 PASS total en headless). Verificado con la barra real: sin sentinel no se crea el log; con sentinel, log normal.
- Lecciones vtables CsWin32 (`allowMarshaling=false`): interfaces = structs con `lpVtbl`; el pattern es `IFace.Interface*` + `IFace*` raw para Release; `IComIID.IID_Guid` para los Guids; wrappers tipo extension no existen — llamar los métodos de `Interface` directo. **CsWin32 no escanea subdirectorios** para NativeMethods.txt extra: un assembly por config (por eso `ViennaBar.Gfx` es proyecto aparte). Los tipos generados son `internal` → `InternalsVisibleTo` para consumirlos desde el proyecto principal.
- Docs de arquitectura: `docs/arquitectura.md` (fuente de verdad), `docs/matriz-cobertura.md`, `docs/f0-spike.md` (gates F0/F1 + bloqueo AOT resuelto en F2.1a).
